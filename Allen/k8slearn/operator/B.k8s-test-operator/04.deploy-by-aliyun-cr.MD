## 04. 构建镜像并部署到集群（阿里云镜像仓库）

部署流程与 A 的套路一致：

- 上传源码到 master
- master 上 `docker build`
- push 到阿里云私有仓库
- 创建/复用 `imagePullSecret`
- `kubectl apply -k` 部署

> 等 B 的工程 `k8s-test-operator/` 代码就绪后，本章补齐可直接执行的命令。
